Using Antigravity, not all configurations and rules can be set through config but needs to be done manually in the IDE. Recommended configuration:

## Terminal

### Allow List Terminal Commands

- `git add -A`
- `git commit -m ""`
- `git diff`
- `git push`
- `git status`
- `yarn build`
- `yarn format`
- `yarn lint`
- `yarn test`
- `yarn ut`

## Automation

Agent Auto-Fix Lints ✅

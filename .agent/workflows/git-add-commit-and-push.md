---
description: This workflow git adds, git commits, and git pushes.
---

1. Git add all files, tracked as well as untracked: `git add -A`.
2. Git commit with a consice but descriptive message `git commit -m`
6. Git push changes: `git push`.